/* Dataset Information */
transform column "$.'name'" to "dataset.title";
transform column "$.'description'" to "dataset.description";
transform column "$.'class'" to "dataset.types";

/* Data Repository Information */
let "dataRepository.ID" = "SCR:006949";
let "dataRepository.name" = "Physiobank";
let "dataRepository.abbreviation" = "Physiobank";
let "dataRepository.homePage" = "http://physionet.org/physiobank/";

/* Publication Information */
transform column "$.'pmid'" to "publication.ID";
transform column "$.'reference_name'" to "publication.title";

/* Access Information */
transform column "$.'url'" to "access.accessURL";
let "access.accesstypes" = "download";
let "access.authorization" = "none";
let "access.authentication" = "none";

/* Organization Information */
let "organization.ID" = "SCR:007345";
let "organization.name" = "PhysioNet";
let "organization.abbreviation" = "PhysioNet";
let "organization.homePage" = "http://www.physionet.org/";


/* Items not included */
/*
transform column "$.'reference_link'" to "reference_link";
transform column "$.'e_uid'" to "e_uid";
transform column "$.'v_uid'" to "v_uid";
transform column "$.'v_uuid'" to "v_uuid";
transform column "$.'v_lastmodified'" to "v_lastmodified";
transform column "$.'section'" to "section";
*/
