/* Dataset Information */
transform column "$.'studynum'" to "dataset.ID";
transform column "$.'study_title'" to "dataset.title";
transform column "$.'investigator'" to "dataset.creator";
transform column "$.'study_description'" to "dataset.description";
transform column "$.'keywords'" to "dataset.keywords[]" apply {{
arr=re.split("\s*,\s*",value,)
result=arr
}};

/* Dataset Distribution */
transform column "$.'release_date'" to "datasetdistribution.dateReleased";

/* Access Information */
transform column "$.'studynum'" to "access.accessURL" apply {{ result = 'http://ctndisseminationlibrary.org/protocols/' + value }};
let "access.accesstypes"  = "download";
let "access.authorization"  = "registration";
let "access.authentication" = "simpleLogin";

/* Data Repository Information */
let "dataRepository.ID" = "SCR:002002";
let "dataRepository.name" = "Clinical Trials Network Data Share";
let "dataRepository.abbreviation" = "CTN";
let "dataRepository.homePage" = "http://datashare.nida.nih.gov/";

/* Organization Information */
let "organization.ID" = "SCR:011440";
let "organization.name" = "National Institute on Drug Abuse";
let "organization.abbreviation"  = "NIDA";
let "organization.homePage" = "http://www.nida.nih.gov/";

/* Taxonomic Information */
let "organism[0].name"  = "Homo sapiens";

/* Items not included */
/*
transform column "$.'e_uid'" to "e_uid";
transform column "$.'v_uid'" to "v_uid";
transform column "$.'v_uuid'" to "v_uuid";
transform column "$.'v_lastmodified'" to "v_lastmodified";
*/
